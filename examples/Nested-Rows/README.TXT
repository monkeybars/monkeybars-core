The Nested-Row sample shows off the nested controller feature using a list of
sorted users. Each row is added in the order it will be displayed in. The layout
handles the rest. Each row is a UserController instance.

Built against Monkeybars 6.2.

Rawr is needed to build this project.

-Logan Barnett